#监控类型，默认使用prometheus实现
monitor.type=prometheus
#监控数据抓取路径，默认ip:port/metrics
monitor.action=metrics
#监控数据抓取端口，默认3333
monitor.port=3333
#另外也提供了Graphite方式的接入，只需要在里按照如下配置即可：
#monitor.type=graphite
#graphite.host=<host>
#graphite.port=<port>